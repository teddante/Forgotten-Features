# Common Module

Shared mod logic will live here once the multi-loader Gradle project is selected.

Expected responsibilities:

- Feature registry and metadata.
- Shared config model.
- Shared data generation helpers.
- Common item, block, entity, loot, recipe, and worldgen definitions where the chosen loader architecture supports it.
- Behavior tests that can run without a specific loader where possible.

